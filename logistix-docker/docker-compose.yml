name: logistix
services:
  logistix:
    build: .
    ports:
      - "48432:48432"
    volumes:
      - logistix-game-data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
volumes:
  logistix-game-data:
