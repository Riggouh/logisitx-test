name: logistix
services:
  logistix:
    build: .
    ports:
      - "47291:47291"
    volumes:
      - logistix-data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
volumes:
  logistix-data:
