name: logistix
services:
  logistix:
    build: .
    ports:
      - "48432:48432"
    volumes:
      - logistix-data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
volumes:
  logistix-data:
